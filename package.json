{
  "name": "garage-layout-planner",
  "version": "1.0.0",
  "description": "Interactive garage layout planning tool",
  "type": "module",
  "scripts": {
    "lint": "eslint 'js/**/*.js'",
    "lint:fix": "eslint 'js/**/*.js' --fix",
    "stylelint": "stylelint 'css/**/*.css'",
    "stylelint:fix": "stylelint 'css/**/*.css' --fix",
    "format": "prettier --write '**/*.{js,css,html,md}'",
    "format:check": "prettier --check '**/*.{js,css,html,md}'"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "stylelint": "^16.2.1",
    "stylelint-config-standard": "^36.0.0"
  }
}
